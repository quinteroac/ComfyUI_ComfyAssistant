import { CanvasColour, DefaultConnectionColors, INodeSlot, ISlotType, IWidgetLocator, Point, Rect } from '../interfaces';
import { LLink } from '../LLink';
import { RenderShape, LinkDirection } from '../types/globalEnums';
/** Base class for all input & output slots. */
export declare abstract class SlotBase implements INodeSlot {
    name: string;
    localized_name?: string;
    label?: string;
    type: ISlotType;
    dir?: LinkDirection;
    removable?: boolean;
    shape?: RenderShape;
    color_off?: CanvasColour;
    color_on?: CanvasColour;
    locked?: boolean;
    nameLocked?: boolean;
    widget?: IWidgetLocator;
    _floatingLinks?: Set<LLink>;
    hasErrors?: boolean;
    /** The centre point of the slot. */
    abstract pos?: Point;
    readonly boundingRect: Rect;
    constructor(name: string, type: ISlotType, boundingRect: Rect);
    abstract get isConnected(): boolean;
    renderingColor(colorContext: DefaultConnectionColors): CanvasColour;
}
