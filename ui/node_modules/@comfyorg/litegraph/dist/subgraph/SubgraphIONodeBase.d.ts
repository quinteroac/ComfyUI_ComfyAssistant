import { Subgraph } from './Subgraph';
import { SubgraphInput } from './SubgraphInput';
import { SubgraphOutput } from './SubgraphOutput';
import { Point, Positionable, Rect } from '../interfaces';
import { NodeId } from '../LGraphNode';
import { ExportedSubgraphIONode, Serialisable } from '../types/serialisation';
export declare abstract class SubgraphIONodeBase implements Positionable, Serialisable<ExportedSubgraphIONode> {
    #private;
    /** The subgraph that this node belongs to. */
    readonly subgraph: Subgraph;
    static margin: number;
    static defaultWidth: number;
    static roundedRadius: number;
    abstract readonly id: NodeId;
    get boundingRect(): Rect;
    selected: boolean;
    pinned: boolean;
    get pos(): Point;
    set pos(value: Point);
    get size(): Point;
    set size(value: Point);
    abstract readonly slots: SubgraphInput[] | SubgraphOutput[];
    constructor(
    /** The subgraph that this node belongs to. */
    subgraph: Subgraph);
    move(deltaX: number, deltaY: number): void;
    /** @inheritdoc */
    snapToGrid(snapTo: number): boolean;
    containsPoint(point: Point): boolean;
    asSerialisable(): ExportedSubgraphIONode;
}
