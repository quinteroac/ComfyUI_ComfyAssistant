#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

cd ui

echo "Running pre-commit checks..."

echo "→ Type checking..."
npm run typecheck || exit 1

echo "→ Linting..."
npm run lint || exit 1

echo "→ Running tests..."
npm test || exit 1

echo "✓ All checks passed!"
