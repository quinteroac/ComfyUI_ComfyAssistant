#!/usr/bin/env sh
# Pre-commit checks are currently disabled.
# To re-enable: see SETUP_AUTOMATED_CHECKS.md and restore the prepare script in ui/package.json.
# Run checks manually before committing: npm run precommit (from ui/)
exit 0
